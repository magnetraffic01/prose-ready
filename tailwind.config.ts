// Tailwind v4 — configuration is handled in app/globals.css via @theme
// This file is kept for tooling compatibility only
import type { Config } from "tailwindcss";
const config: Config = {};
export default config;
